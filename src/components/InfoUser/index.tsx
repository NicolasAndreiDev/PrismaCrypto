import styles from './InfoUser.module.scss'

export default function InfoUser() {
    return(
        <div className={styles.container}>
            <div className={styles.top}>
                <img src={'/perfil.jpg'} className={styles.foto}/>
            </div>
            <select>

            </select>
        </div>
    )
}